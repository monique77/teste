name: Github Actions - Push branches ignore template basic
run-name: Running - ${{ github.event_name }}
on: 
  push:
    branches:
      - main
      
jobs:
  on-push:
    runs-on:   ubuntu-latest
    steps:
      - name: Comentário
        run: |
          echo "Comentáro do workflow PUSH"
